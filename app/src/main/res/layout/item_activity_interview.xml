<?xml version="1.0" encoding="utf-8"?>
<!-- Recent Activity: Interview row item -->
<androidx.cardview.widget.CardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginBottom="10dp"
    android:clickable="true"
    android:focusable="true"
    android:stateListAnimator="@animator/scale_on_press"
    app:cardCornerRadius="16dp"
    app:cardElevation="3dp"
    app:cardBackgroundColor="@color/bg_white">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:paddingHorizontal="16dp"
        android:paddingVertical="14dp">

        <!-- Left icon circle -->
        <FrameLayout
            android:id="@+id/fl_icon"
            android:layout_width="42dp"
            android:layout_height="42dp"
            android:background="@drawable/bg_accent_badge"
            android:padding="10dp"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintBottom_toBottomOf="parent">

            <ImageView
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:src="@drawable/ic_mic"
                app:tint="@color/accent_indigo" />
        </FrameLayout>

        <!-- Title: "Interview" label -->
        <TextView
            android:id="@+id/tv_activity_title"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginStart="14dp"
            android:layout_marginEnd="8dp"
            android:text="Interview"
            android:textColor="@color/text_primary"
            android:textSize="14sp"
            android:textStyle="bold"
            app:layout_constraintStart_toEndOf="@+id/fl_icon"
            app:layout_constraintEnd_toStartOf="@+id/tv_score_pill"
            app:layout_constraintTop_toTopOf="@+id/fl_icon" />

        <!-- Sub-label: role applied for -->
        <TextView
            android:id="@+id/tv_activity_role"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginStart="14dp"
            android:layout_marginEnd="8dp"
            android:layout_marginTop="2dp"
            android:text="Software Engineer"
            android:textColor="@color/text_secondary"
            android:textSize="12sp"
            android:maxLines="1"
            android:ellipsize="end"
            app:layout_constraintStart_toEndOf="@+id/fl_icon"
            app:layout_constraintEnd_toStartOf="@+id/tv_score_pill"
            app:layout_constraintTop_toBottomOf="@+id/tv_activity_title" />

        <!-- Date label -->
        <TextView
            android:id="@+id/tv_activity_date"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginStart="14dp"
            android:layout_marginEnd="8dp"
            android:layout_marginTop="3dp"
            android:text="Mar 16, 2026"
            android:textColor="@color/text_muted"
            android:textSize="11sp"
            app:drawableStartCompat="@drawable/ic_calendar"
            app:drawableTint="@color/text_muted"
            android:drawablePadding="4dp"
            app:layout_constraintStart_toEndOf="@+id/fl_icon"
            app:layout_constraintEnd_toStartOf="@+id/tv_score_pill"
            app:layout_constraintTop_toBottomOf="@+id/tv_activity_role" />

        <!-- Score pill (right side) -->
        <TextView
            android:id="@+id/tv_score_pill"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:background="@drawable/bg_accent_badge"
            android:paddingHorizontal="12dp"
            android:paddingVertical="6dp"
            android:text="72/100"
            android:textColor="@color/accent_indigo"
            android:textStyle="bold"
            android:textSize="13sp"
            app:layout_constraintEnd_toStartOf="@+id/iv_arrow"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintBottom_toBottomOf="parent" />

        <!-- Arrow -->
        <ImageView
            android:id="@+id/iv_arrow"
            android:layout_width="18dp"
            android:layout_height="18dp"
            android:src="@drawable/ic_arrow_right"
            app:tint="@color/text_muted"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintBottom_toBottomOf="parent" />

    </androidx.constraintlayout.widget.ConstraintLayout>
</androidx.cardview.widget.CardView>
